/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

module.exports = {
  plugins: {
    'autoprefixer': {},
    // other plugins
  },
};


export default config;
